/**
 * Sonderfolien — Normalisierung (Re-Export für gemeinsame Renderer-Pipeline).
 */

export {
  getSpecialSlideConfig,
  getCurrentSpecialSlide,
  activeSpecialSlideKind,
  isCountdownSpecialActive,
} from "../eventSpecialSlides.js";
